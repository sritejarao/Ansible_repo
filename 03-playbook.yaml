- name: install nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx at node1
    ansible.builtin.package:
    name: nginx
    state: present
  - name: start the nginx
    ansible.builtin.service:
    name: nginx
    state: started
    enable: yes